import { Typography } from "@mui/material";
import styled from "styled-components";

export const StyledPageTitle = styled(Typography)(() => ({
    fontSize: "3.2rem",
    fontWeight: 700,
    lineHeight: "4rem",
    fontFamily: 'Cairo',
    textAlign: 'left'
}));
export const StyledThinPageTitle = styled(Typography)(() => ({
    fontSize: "1.2rem",
    fontWeight: 700,
    lineHeight: "4rem",
    fontFamily: 'Cairo',
    textAlign: 'left'
}));