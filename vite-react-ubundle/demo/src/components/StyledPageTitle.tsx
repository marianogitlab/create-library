import { Box } from "@mui/material";
import styled from "styled-components";

export const StyledPageTitle = styled(Box)(() => ({
    fontSize: "3.2rem",
    fontWeight: 700,
    lineHeight: "4rem",
    fontFamily: 'Cairo',
    textAlign: 'left'
}));
export const StyledThinPageTitle = styled(Box)(() => ({
    fontSize: "1.2rem",
    fontWeight: 700,
    lineHeight: "4rem",
    fontFamily: 'Cairo',
    textAlign: 'left'
}));