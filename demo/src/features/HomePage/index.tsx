import { Box } from "@mui/material";
import { VdsTextField } from "vds";

export const HomePage = () => {

    return (
        <Box>
            HomePage
            <VdsTextField label="Componente condiviso sotto vds" />
        </Box>
    );
}