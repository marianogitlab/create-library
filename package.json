{
  "name": "vds",
  "version": "1.0.0",
  "description": "Vapor Design System for React",
  "author": "Mariano De Luca",
  "license": "ISC",
  "private": true,
  "workspaces": ["lib", "demo"],
  "scripts": {
    "dev": "yarn && cd ./lib && yarn run build && cd .. && npx concurrently -n watch-js,watch-css,watch-node \"yarn run lib\" \"yarn run demo\"",
    "lib": "cd ./lib && yarn run start",
    "demo": "cd ./demo && yarn run dev --open"
  }
}
